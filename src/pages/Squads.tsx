
import Layout from "@/components/Layout";
import SquadContainer from "@/components/squads/SquadContainer";

const Squads = () => {
  return (
    <Layout>
      <SquadContainer />
    </Layout>
  );
};

export default Squads;
